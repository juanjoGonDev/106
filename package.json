{
  "name": "minuto-106",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build:config": "node scripts/generate-config.mjs",
    "check": "node --check public/app.js && node --check scripts/generate-config.mjs && node --check scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
